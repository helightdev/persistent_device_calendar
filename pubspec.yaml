name: persistent_device_calendar
description: Persistence wrapper for device_calendar
version: 0.0.1
repository: https://github.com/helightdev/persistent_device_calendar

environment:
  sdk: '>=2.18.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  device_calendar: ^4.2.0
  shared_preferences: ^2.0.15
  timezone: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: {}
